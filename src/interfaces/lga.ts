export interface Lga{
    name: string, 
    creation_date: string, 
    location: { latitude: string, longitude: string },
    total_area: string, 
    population: string, 
    postal_code: string, 
}
